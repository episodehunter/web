import { styled } from '@material-ui/core'

export const BottomContentOnImage = styled('div')({
  padding: 10,
  flexGrow: 1,
  background: 'linear-gradient(rgba(0, 0, 0, 0), rgba(0, 0, 0, 0.5) 30%)'
})
